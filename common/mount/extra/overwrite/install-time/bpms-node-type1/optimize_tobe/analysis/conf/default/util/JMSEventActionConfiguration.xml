<?xml version="1.0" encoding="UTF-8"?>

<properties>
    <!--
    <property name="action">
        String containing the unique name for this action configuration
        NOTE: Configuration parameters must remain in the order specified by this example.
        <string meta="name">Name Required</string>
        String containing the event type from the Event Mapping definition in the target AE
        <string meta="eventType">Event Mapping base type Required</string>
        [Optional Parameters]
        <string meta="destination">queue/dca_event</string>

        [Optional but Requires destination]
        <string meta="url">broker://localhost:6849/Broker #1/analysis</string>
        <string meta="url">nsp://localhost:9000</string>

        [Optional but if any are used all above and all below must be used]
        <string meta="description">Some description</string>
        true specifies the action is invoked when the rule instance returns to the "in compliance" state
        <boolean meta="sendOnIn">false</boolean>
        true specifies the action is invoked when the rule instance goes to the "out of compliance" state
        <boolean meta="sendOnOut">true</boolean>
        true specifies the action will perform when invoked
        <boolean meta="active">true</boolean>

        One or more of the elements in the list are required
        <list>
            String containing of the name of the rule definition of the instance that fired
            <element><string>RuleName</string></element>
            String containing of the name of the rule instance that fired
            <element><string>RuleInstanceName</string></element>
            <element><string>ViolationCount</string></element>
            String containing the description of the rule definition
            <element><string>RuleDescription</string></element>
            String containing the evaluation of the rule
            <element><string>RuleEvaluation</string></element>
            String containing the SLA setting from the rule definition
            <element><string>RuleSla</string></element>
            String containing the Customer specified in the rule definition
            <element><string>RuleCustomer</string></element>
            String containing the rule status from the rule instance
            <element><string>RuleStatus</string></element>
            String containing the severity setting from the rule definition
            <element><string>RuleSeverity</string></element>
            String form of the time that the rule went out of compliance
            <element><string>Time</string></element>
            Allows you to specify a particular field from the underlying data of the monitor. For example,
            if the rule is on business data containing a ProcessInstanceId field, you would change
            the brackets and contents of the bracket to ProcessInstanceId
            <element><string>[An Attribute from the KPI or Event Map at time of Violation]</string></element>
        </list>

    </property>
    -->
</properties>
